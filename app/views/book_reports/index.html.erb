<h1>Book Reports</h1>
<div class="card">
  <div class="card-body">
    <% current_user.book_reports.each do |book_report| %>
      <a href="/books/<%= book_report.book_id %>"><%= book_report.title %> </a><br><br>
    <% end %>
  </div>
</div>
